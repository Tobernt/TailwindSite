# TailwindSite

En enkel CV-landningssida byggd med Tailwind CSS via CDN. Öppna `index.html` i valfri statisk server, till exempel:

```bash
python -m http.server 5173
```

och gå sedan till http://localhost:5173 i din webbläsare.
